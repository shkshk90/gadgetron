if (NOT USE_AMALGAMATED)
    add_executable(ktsense_radial_2d  main.cpp)

    target_link_libraries(ktsense_radial_2d gadgetron_toolbox_gpu gadgetron_toolbox_hostutils ${CUDA_LIBRARIES})

    install(TARGETS ktsense_radial_2d DESTINATION bin COMPONENT main)
else()
    if (USE_SYCL)
        add_executable(ktsense_radial_2d main_amalgamated.dp.cpp)
        target_compile_options(ktsense_radial_2d PRIVATE -fsycl -fsycl-targets=nvptx64-nvidia-cuda -Xsycl-target-backend --cuda-gpu-arch=sm_80)
        target_link_options(ktsense_radial_2d PRIVATE -fsycl -fsycl-targets=nvptx64-nvidia-cuda -Xsycl-target-backend --cuda-gpu-arch=sm_80)
        target_link_libraries(ktsense_radial_2d ${GADGETRON_AMALGAMATED_LIBS} sycl OpenCL stdc++fs mkl_sycl mkl_core mkl_sycl_blas mkl_sycl_sparse)
    else()
        add_executable(ktsense_radial_2d main_amalgamated.cu)
        target_link_libraries(ktsense_radial_2d ${GADGETRON_AMALGAMATED_LIBS})
    endif()
    install(TARGETS ktsense_radial_2d DESTINATION bin COMPONENT main)
endif()
